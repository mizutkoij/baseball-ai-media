# 🚀 **SystemD 24/7自動運転 - 最終セットアップ**

## ✅ **準備完了状況**
- ✅ SystemD設定ファイル作成済み
- ✅ 自動インストールスクリプト準備完了
- ✅ Discord webhook設定済み

---

## 🎯 **最終実行手順（1回だけ）**

### **手動実行で24/7運転開始:**

```bash
cd ~/baseball-ai-media/systemd_config
sudo ./install_systemd.sh
```

**これで完了！** 以下が自動開始されます：

- ✅ **Step1**: 15分おき スケジュール増分収集
- ✅ **Step2**: 60分おき インデックス抽出  
- ✅ **Step3**: 常駐 投球ログ収集（Discord通知付き）

---

## 📊 **監視・確認コマンド**

```bash
# タイマー確認
systemctl list-timers | grep npb-step

# Step3常駐サービス確認
systemctl status npb-step3-live

# リアルタイムログ監視
journalctl -u npb-step3-live -n 200 -f

# ファイルログ確認
tail -f ~/baseball-ai-media/logs/*.log
```

---

## 🛠️ **運用管理コマンド**

```bash
# サービス停止
sudo systemctl stop npb-step1.timer npb-step2.timer npb-step3-live.service

# サービス開始
sudo systemctl start npb-step1.timer npb-step2.timer npb-step3-live.service

# サービス再起動
sudo systemctl restart npb-step3-live.service

# ログローテーション（必要時）
sudo logrotate -f /etc/logrotate.conf
```

---

## 🎉 **完成！**

**24/7自動運転システム稼働準備完了**

1. **礼儀正しい収集**: 30s間隔 + robots.txt順守
2. **自動復旧**: 障害時の自動再起動
3. **Discord可視化**: リアルタイムCSV通知
4. **Heatmap連携**: 投球座標データ供給
5. **完全無人運用**: システム再起動時も自動開始

**`sudo ./install_systemd.sh` 実行で即座に本番運用開始！**